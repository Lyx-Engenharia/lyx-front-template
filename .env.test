# Variáveis de ambiente pra `vitest` / `npm run audit:all`.
# Valores fakes válidos pra passar a build de teste.
# NÃO contém segredos reais — pode (e deve) ser commitado.

NODE_ENV=test

# URL fake do monolito (testes não devem chamar HTTP real).
NEXT_PUBLIC_API_URL=http://localhost:3000
